replicaCount: 1

image:
  repository: fzshaik8297/Webapp
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

service:
  type: NodePort
  port: 8080
  targetport: 8080

container:
  name: fayaz
